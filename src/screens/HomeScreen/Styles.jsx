import { StyleSheet} from 'react-native';
import  { 
    background, 
    primary500,
    primary700,
    secundary200
}  from "../../theme/index"

const styles = StyleSheet.create({
    container: {
		flex: 1,
	},
});

export default styles;